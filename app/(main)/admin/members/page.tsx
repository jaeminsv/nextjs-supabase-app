/**
 * Member management page — admin-only.
 *
 * Tabs: pending approvals / all members.
 * Pending tab: applicant info + approve/reject buttons.
 * All members tab: name, role badge, join date + promote to admin button.
 *
 * Phase 2 (Task 009): Replace with actual member management UI.
 */
export default function MemberManagementPage() {
  return (
    <div className="container mx-auto p-4">
      <h1 className="text-2xl font-bold">회원 관리</h1>
      <p className="mt-2 text-muted-foreground">
        회원 승인 및 관리 기능이 표시될 예정입니다.
      </p>
    </div>
  );
}

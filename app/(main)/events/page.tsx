/**
 * Event list page — shows all published events.
 *
 * Features an upcoming/past toggle filter, event cards with
 * title, date, location, capacity, and RSVP status.
 *
 * Phase 2 (Task 005): Replace with actual event list UI with dummy data.
 */
export default function EventsPage() {
  return (
    <div className="container mx-auto p-4">
      <h1 className="text-2xl font-bold">이벤트</h1>
      <p className="mt-2 text-muted-foreground">
        동문회 이벤트 목록이 표시될 예정입니다.
      </p>
    </div>
  );
}

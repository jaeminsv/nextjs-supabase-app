/**
 * Dashboard page — the main home screen for authenticated members.
 *
 * Shows upcoming events at a glance, RSVP status badges,
 * and unpaid fee alerts.
 *
 * Phase 2 (Task 005): Replace with actual dashboard UI with dummy data.
 */
export default function DashboardPage() {
  return (
    <div className="container mx-auto p-4">
      <h1 className="text-2xl font-bold">대시보드</h1>
      <p className="mt-2 text-muted-foreground">
        예정 이벤트 요약이 표시될 예정입니다.
      </p>
    </div>
  );
}
